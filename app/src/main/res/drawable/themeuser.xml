<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/orange_user"/>
    <corners
        android:radius="20dp"/>
    <!-- độ rộng viền và màu -->
    <stroke
        android:width="1dp"
        android:color="@color/color_basic_1" />

</shape>